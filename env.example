# Copy this file to .env and fill in your local MySQL credentials.
# .env itself must NEVER be committed (already covered in .gitignore).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=recruitment_dw
